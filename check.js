const fs = require('fs'); const content = fs.readFileSync('index.html', 'utf8'); const scripts = [...content.matchAll(/<script[\s\S]*?>([\s\S]*?)<\/script>/g)].map(m => m[1]); scripts.forEach((s, i) => { fs.writeFileSync('script_' + i + '.js', s); try { new require('vm').Script(s); console.log('Script ' + i + ' OK'); } catch(e) { console.log('Script ' + i + ' ERROR: ' + e.message); } });
